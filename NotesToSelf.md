- TextEdits are unable to center text at time of writing, but Labels can.
	Could I use a textedit behind a label, hide the text in the textedit and transfer it to the label?
	Maybe.
	It would be easiest(?) to have the labels be children of the text fields, and the text gets passed to them
	Labels need to be informed whether their related TextEdit field is editable, and change their font accordingly

- I can use normal textedits for Notes mode, and the above for regular mode. I could set it to swap between them using the N key.
	Downside: I'd need twice as many text edits, but I'm sure it'd be fine.
